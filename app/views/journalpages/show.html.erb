<div class="flex flex-col items-center min-h-screen mt-0">
  <div class="mb-4 w-full max-w-3xl">
    <h2 class="text-lg font-medium mb-4">
      <%= @journalpage.prompt %>
    </h2>
    <p class="textarea textarea-bordered w-full h-96 p-4 bg-base-100 text-secondary-content rounded-lg leading-relaxed">
      <%= @journalpage.answer %>
    </p>
  </div>
  <div class="form-actions mt-4">
    <%= link_to "Back to Pages", journal_journalpages_path(@journal), class: "btn btn-primary" %>
  </div>
</div>
